

spring.datasource.url=${DB_URL:jdbc:postgresql://postgres:5432/inspection}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

app.upload.dir=${UPLOAD_DIR:/app/shared/images/}
python.service.url=${MODEL_URL:http://model:5000}
